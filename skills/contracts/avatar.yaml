kind: avatar
contract_version: "1.0"
display_name: 数字人视频生成
provider: giggle-generation-tv-avatar-video
name: giggle-generation-tv-avatar-video
required_inputs:
  image_path: string
  audio_path: string
  layout: seated_studio_anchor
optional_inputs:
  - text
required_outputs:
  - video_path
  - task_id
supported_aspect_ratios:
  - "9:16"
max_duration_seconds: 50
real_generation_enabled: false
primary_mode: image_plus_audio
fallback_mode: image_plus_text
safety_constraints:
  - 固定主持人参考图与TTS音频驱动数字人
  - 不冒充真实新闻记者或专业权威
  - 保持主持人形象和演播室一致性
